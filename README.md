# **Real-time data streaming with React and Websockets**

To run app in dev mode, run:

```bash
npm i
npm run dev
```

To create a bundle, run:

```bash
npm run build
# to preview production version
npm run preview
```
